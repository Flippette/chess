[package]
name = "chess"
version = "0.1.0"
edition = "2021"
authors = ["Flippette <quangdatle2006@outlook.com>"]
license = "GPL-3.0-only"

[dependencies]
anyhow = "1"

[profile.release]
lto = "thin"
panic = "abort"
strip = "symbols"
